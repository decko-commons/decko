- :name: "*input type"
  :type: :setting
  :codename: :input_type
  :fields:
    :right:
      :fields:
        # TODO: refactor so that we don't need a compound card to have a codename
        :content_options:
          :codename: :input_options
          :content: |-
            overridden
        :input_type: radio
