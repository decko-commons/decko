- :name: "*input type"
  :type: :setting
  :codename: :input_type
  :subfields:
    :right:
      :subfields:
        # TODO: refactor so that we don't need a compound card to have a codename
        :content_options:
          :codename: :input_options
        :input_type: radio
