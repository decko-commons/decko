source 'http://rubygems.org'

<% if @gem_path.present? %>
path "<%= @gemfile_gem_path %>" do
  gem 'card-mod-defaults'
  gem 'decko'
end
<% else %>
gem 'card-mod-defaults'
gem 'decko'
<% end %>

gem '<%= database_gemfile_entry.name %>'<%= %(, '#{database_gemfile_entry.version}') if database_gemfile_entry.version %>

# WEBSERVER
# To run a simple deck at localhost:3000, you can use thin (recommended), unicorn,
# or (Rails' default) Webrick
gem 'thin'
# gem 'unicorn'

# BACKGROUNDING
# The daemons gem is needed to run tasks like sending notifications in a background process.
# See config/application.rb for configuration details.
# gem 'daemons'


gem 'card-mod-monkey_test', group: :test
gem 'card-mod-monkey_development', group: :development
gem 'cypress-on-rails', '~> 1.4', group: :cypress, :test

<%- if spring_install? %>
# Spring speeds up development by keeping your application running in the background.
# Read more: https://github.com/rails/spring
gem 'listen', '3.0.6'
gem 'spring'
gem 'spring-commands-rspec'
gem 'spring-commands-cucumber'
<% end -%>

group :debug do
  gem 'pry-rails'
  gem 'pry-rescue'
  gem 'pry-stack_explorer'
  gem 'pry-byebug'
end

group :test do
  gem 'card-mod-platypus_test'
  gem 'capybara-select2', git: 'https://github.com/brunocavalcante/capybara-select2'
end

gem 'ruby-prof', group: :profile  # profiling

# The following allows simple (non-gem) mods to include gems via a Gemfile.
# You may need to alter this code if you move such mods to an unconventional location.
Dir.glob("mod/**/Gemfile").each { |gemfile| instance_eval File.read(gemfile) }
