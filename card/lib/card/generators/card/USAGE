Description:
    The 'card new' command creates a new Card deck with a default
    directory structure and configuration at the path you specify.

Example:
    card new ~/Poker/FullHouse

    This generates a new deck in ~/Poker/FullHouse.

