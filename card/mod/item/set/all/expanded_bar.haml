- bar_classes = css_classes classy("bar"), card.safe_set_keys
.expanded-bar.border.with-fancy-border
  .bar-top.border.d-flex.justify-content-between.p-0.nodblclick{class: bar_classes}
    %div{class: classy("bar-left")}
      = render :bar_left
    %div{class: classy("bar-right")}
      = render :bar_right, hide: :short_content
      = render :bar_expanded_nav
  .bar-bottom
    = render :bar_bottom
