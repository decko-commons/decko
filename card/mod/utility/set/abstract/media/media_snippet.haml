.media{media_opts}
  .image-box{class: [classy("media-left"), size]}
    %a{href: "#"}
      %img.media-object{class: size, src: src.html_safe, alt: alt}
  .media-body
    - if title != ""
      %h5.media-heading
        = title
    = text
